<VirtualHost *:80>
    ServerName {{hostname}}
    DocumentRoot /var/www/{{customer.name}}/{{hostname}}

    ErrorLog "|/usr/bin/cronolog ${APACHE_LOG_DIR}/{{customer.name}}/{{hostname}}/error.log-%Y%m%d"
    CustomLog "|/usr/bin/cronolog ${APACHE_LOG_DIR}/{{customer.name}}/{{hostname}}/access.log-%Y%m%d" combined

    <FilesMatch ".+\.php$">
        SetHandler "proxy:unix:/run/php/{{hostname}}.sock|fcgi://localhost"
    </FilesMatch>

    <Directory /var/www/{{customer.name}}/{{hostname}}>
        AllowOverride All
    </Directory>
</VirtualHost>
